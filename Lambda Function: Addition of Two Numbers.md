# Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
```
a=int(input())
b=int(input())
f=lambda a,b:a+b
print(f(a,b))



```
## Output
<img width="1919" height="780" alt="Screenshot 2026-05-31 183115" src="https://github.com/user-attachments/assets/fc21ef72-8b6d-4613-b647-789bd065b120" />

## Result
The lambda function was successfully used to add two given numbers.
The correct sum of the numbers was computed and displayed as the output.
